@model LearningProject.Models.ErrorLog

@{
    ViewData["Title"] = "Editare Jurnal Erori";
}

<div class="container mt-4">
    <div class="card shadow-lg border-0">
        <div class="card-header bg-primary text-white d-flex justify-content-between align-items-center">
            <h4 class="mb-0">Editare Jurnal Erori</h4>
            <a asp-action="Index" class="btn btn-light btn-sm">
                <i class="bi bi-arrow-left-circle"></i> Înapoi la listă
            </a>
        </div>

        <div class="card-body">
            <form asp-action="Edit">
                <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>
                <input type="hidden" asp-for="Id" />

                <div class="mb-3">
                    <label asp-for="ErrorMessage" class="form-label fw-semibold"></label>
                    <input asp-for="ErrorMessage" class="form-control shadow-sm" placeholder="Introduceți mesajul de eroare" />
                    <span asp-validation-for="ErrorMessage" class="text-danger"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="StackTrace" class="form-label fw-semibold"></label>
                    <input asp-for="StackTrace" class="form-control shadow-sm" placeholder="Introduceți stack trace-ul" />
                    <span asp-validation-for="StackTrace" class="text-danger"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="DateOccurred" class="form-label fw-semibold"></label>
                    <input asp-for="DateOccurred" class="form-control shadow-sm" type="datetime-local" />
                    <span asp-validation-for="DateOccurred" class="text-danger"></span>
                </div>

                <div class="d-flex justify-content-end">
                    <button type="submit" class="btn btn-success me-2">
                        <i class="bi bi-save"></i> Salvează
                    </button>
                    <a asp-action="Index" class="btn btn-secondary">
                        <i class="bi bi-x-circle"></i> Anulează
                    </a>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
