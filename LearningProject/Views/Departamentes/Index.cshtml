@model IEnumerable<LearningProject.Models.Departamente>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Denumire_departament)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Denumire_departament)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.id_departamente">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.id_departamente">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.id_departamente">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
